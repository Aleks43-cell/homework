

const numbers = [1, 5, 10, 15, 20]

function solid(numbers) {
    for (let i = 0; i < numbers.length; i++) {
        if (numbers[i] > 10) {
            console.log(numbers[i])
        }
    }
}
    solid(numbers)
